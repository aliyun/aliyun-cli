# 私钥、公钥轮转

# 你发现的是真问题——而且比你想的更严重

先把结论说清楚:

> **你现在的架构里,"私钥泄露"没有优雅出路。** 而且我上一条回复里说的"双签重叠窗口"**只适用于计划内轮换,对泄露场景完全无效**——这是两个必须分开看的问题。

---

## 一、为什么双签救不了泄露

**计划内轮换**(密钥到期、换算法):老密钥仍然可信,双签只是为了兼容,没问题。

**泄露场景**:你双签(A+B),老客户端只认 A → **攻击者也只需要用泄露的 A 签名,老客户端照样接受**。双签把攻击者也一起兼容进来了。

于是你被夹在两个都不能接受的选项之间:

| 选择 | 后果 |
| --- | --- |
| 继续用泄露的 A 签(或双签) | 老客户端**仍然可被攻击**,泄露等于没处理 |
| 只用新密钥 B 签 | 老客户端**全部砖化**,连"升级到能认 B 的版本"这条路都走不通(因为升级包本身验不过) |

第二个选项还有个致命的死锁:**用户没法通过 CLI 自救**,只能去官网手动重装。对你这个场景(插件生态)更糟——用户核心 CLI 不升级,但插件索引必须继续更新,你一换签名密钥,所有老客户端的插件安装/升级立刻全断。

---

## 二、根因:你把信任锚和二进制生命周期焊死了

```plaintext
硬编码公钥  ──绑定──> 二进制版本  ──依赖──> 用户是否升级

```

信任锚的更新频率(泄露时需要**立刻**换)和二进制的更新频率(用户可能**一年不升**)严重不匹配。这个结构必然出问题。

**正确做法:让信任锚可以在不更换二进制的前提下更新。** 这就是分层信任(TUF 的核心)存在的唯一理由。

---

## 三、分层:客户端只钉 Root,签名密钥可以随时换

```plaintext
┌─────────────────────────────────────────────────────┐
│  Root Key ×3  (离线保管,3-of-3 分散在不同地点)      │
│  ├─ 有效期 3–5 年,一年可能只用 1–2 次               │
│  └─ 唯一职责:签署 root.json = "现在谁有权签东西"   │
└──────────────────────┬──────────────────────────────┘
                       │ 签
                       ▼
              ┌────────────────┐
              │   root.json    │  ← 可随时更新,客户端在线学习
              │  version: 7    │
              │  roles: {...}  │
              └────────┬───────┘
                       │ 授权
        ┌──────────────┼──────────────┐
        ▼              ▼              ▼
   Manifest Key   Plugin Key    Timestamp Key
   (KMS,90天轮换) (KMS)         (KMS,短周期)
        │              │              │
        ▼              ▼              ▼
   manifest.json  plugin-index   timestamp.json

```

**客户端硬编码的只有 Root 公钥**(以及一份内嵌的 root.json 快照作为 bootstrap)。

### 这直接解决了你的顾虑

> "换了私钥,用户侧代码不更新是不是就废了"

**不会。** 换掉的是 Manifest Key / Plugin Key,而客户端信任的是 Root。流程:

1.  Root Key 签一份新的 `root.json`(version 8),里面**移除泄露的 key,加入新 key**
    
2.  老客户端下次联网,先拉 `root.json`,用**内置 Root 公钥**验签通过
    
3.  客户端立即学到"新的签名密钥是 B,旧的 A 已不在授权列表里"
    
4.  此后 A 签的任何东西**一律拒绝**,B 签的正常接受
    
5.  **用户一行代码没改,二进制没升级,插件继续正常升级**
    

泄露从"全网砖化 or 全网裸奔"变成"**一次 root 签名 + 用户无感**"。

### root.json 长什么样

```json
{
  "signed": {
    "type": "root",
    "version": 8,
    "expires": "2026-09-01T00:00:00Z",
    "keys": {
      "r1": { "algo": "ed25519", "pub": "d75a98..." },
      "r2": { "algo": "ed25519", "pub": "3d4017..." },
      "r3": { "algo": "ed25519", "pub": "8bc4f1..." },
      "m2": { "algo": "ed25519", "pub": "aa77e0..." },
      "p2": { "algo": "ed25519", "pub": "1f3ca9..." }
    },
    "roles": {
      "root":      { "keyids": ["r1","r2","r3"], "threshold": 2 },
      "manifest":  { "keyids": ["m2"],           "threshold": 1 },
      "plugins":   { "keyids": ["p2"],           "threshold": 1 },
      "timestamp": { "keyids": ["t1"],           "threshold": 1 }
    }
  },
  "signatures": [
    { "keyid": "r1", "sig": "..." },
    { "keyid": "r2", "sig": "..." }
  ]
}

```

注意 `"threshold": 2` —— **Root 用 2-of-3 阈值签名**。这样单个 Root 私钥泄露/丢失都不致命,这是分层方案能成立的前提(否则你只是把"单点"从签名密钥挪到了 Root 密钥)。

### 客户端验证逻辑

```go
// 编译期内嵌:Root 公钥 + 出厂时的 root.json 快照
//go:embed trust/root.json

var embeddedRoot []byte


func LoadTrustedRoot(src Source) (*Root, error) {
    // 1) 本地信任起点:优先用持久化的(更新),否则用内嵌的
    local := loadPersistedRoot()
    if local == nil || local.Version < parseRoot(embeddedRoot).Version {
        local = parseRoot(embeddedRoot)   // 二进制升级后可能比本地缓存更新
    }

    // 2) 逐版本前推,不允许跳跃(每一版都要被上一版验证)
    for {
        next, err := src.GetRoot(local.Version + 1)
        if errors.Is(err, ErrNotFound) { break }   // 已是最新
        if err != nil { return nil, err }

        // 关键:新 root 必须同时满足
        //   a) 被【当前信任的 root】的阈值签名  → 防止外人伪造
        //   b) 被【它自己声明的 root】的阈值签名 → 防止签出一个没人能用的 root
        if err := verifyThreshold(next.SignedBytes, next.Signatures,
                                  local.Role("root")); err != nil {
            return nil, fmt.Errorf("root chain broken at v%d: %w", next.Version, err)
        }
        if err := verifyThreshold(next.SignedBytes, next.Signatures,
                                  next.Role("root")); err != nil {
            return nil, errRootSelfSignFailed
        }
        if next.Version <= local.Version { return nil, errRootRollback }

        local = next
        persistRoot(local)     // 单调持久化,永不回退
    }

    if time.Now().After(local.Expires) {
        return nil, errRootExpired    // fail-closed
    }
    return local, nil
}

// 之后再用 local.Role("manifest") / local.Role("plugins") 去验各自的文件

```

三条不变量,少一条就有洞:

*   **链式验证**:v6→v7→v8 逐级验,不能直接跳到 v8(否则攻击者可以拿一份被吊销 Root 签的 v99 骗你)
    
*   **版本单调持久化**:见过 v8 就永不接受 v7(防 Root 回滚)
    
*   **自签名检查**:新 root 也必须被它自己列出的 Root 密钥签够阈值(防止你误签出一份没人能继续维护的 root,直接锁死全网)
    

---

## 四、泄露应急剧本

假设 Manifest Key 泄露:

| 时间 | 动作 |
| --- | --- |
| T+0 | 检测(Rekor 监控告警 / KMS 审计日志出现非预期调用 / 内部发现) |
| T+30m | 撤销 CI 对该 KMS key 的 IAM 权限;暂停 release 流水线 |
| T+2h | 在 KMS 生成新 Manifest Key `m3`;召集 2 位 Root 持有人 |
| T+4h | Root 2-of-3 签出 `root.json` v9:移除 `m2`,加入 `m3` |
| T+4h | 发布 root.json v9 → **客户端下次联网自动生效,无需升级** |
| T+5h | 用 `m3` 重签当前所有 manifest / plugin-index,`index_version` 递增 |
| T+6h | 公告 + Rekor 日志比对,列出泄露窗口内所有签名事件,确认有无伪造制品 |

对比不分层的情况:第 4 步你只能选"砖化全网"或"什么都不做"。

---

## 五、必须承认的残余风险(以及怎么压缩)

分层不是万能的。有一个**理论上无法消除**的缺口:

> 攻击者若完全控制某个用户的下载通道,他可以**扣住 root.json v9 不给**,继续投喂"旧 root v8 + 用泄露密钥签的恶意 manifest"。客户端从自己的视角看,一切合法。

这不是实现缺陷,而是离线验证的固有极限——**你没法给一个收不到你消息的客户端送去吊销通知**。能做的是把窗口压到最小:

**1. 短周期 timestamp(最关键)**

Root 是离线密钥,不能天天签,所以 `root.json` 的 expires 只能设几个月。但你可以加一个在线的短命角色:

```json
// timestamp.json —— 在线 KMS 签,每 6 小时重签一次
{
  "signed": {
    "type": "timestamp",
    "version": 10432,
    "expires": "2025-06-02T20:00:00Z",   // 只有 12 小时
    "meta": {
      "root.json":     { "version": 9, "sha256": "..." },
      "manifest.json": { "version": 482, "sha256": "..." }
    }
  },
  "signatures": [{ "keyid": "t1", "sig": "..." }]
}

```

客户端规则:

```go
if now.After(ts.Expires) {
    return errStale   // 攻击者最多能冻结你 12 小时
}
// timestamp 声明 root 应该是 v9,但我本地只有 v8 → 必须去取 v9,取不到就中止
if ts.Meta["root.json"].Version > local.Version {
    return errRootUpdateRequired   // 堵死"扣住 root 不给"这条路
}

```

有了这个,**"扣住吊销通知"的攻击窗口从"永久"缩到"一个 timestamp 周期"**。Timestamp Key 本身泄露危害有限——它只能声明版本和 hash,改不了内容(内容 hash 链在 root 授权的 manifest 上)。

**2. 二进制内嵌 root 快照 + 最低版本下限**

```go
const minAcceptableRootVersion = 9   // 编译期常量

```

新发布的二进制天然带着 v9,**新装用户不受影响**;同时拒绝任何低于 v9 的 root,即使攻击者拿着老 Root 签名也没用。

**3. 拒绝服务式兜底**

对于"确实联系不上、且拒绝升级"的客户端,最终状态应该是**明确地停止工作**,而不是继续裸奔:

```plaintext
✗ 无法获得有效的信任元数据(root.json 已于 2025-05-12 过期)

  这可能意味着:
  • 你的网络无法访问 dl.example.com
  • 你使用的镜像/代理已长期未同步
  • 或该客户端版本已不再受支持

  请从官方渠道重新安装:
    https://example.com/install   (SHA-256 公布于该页面)
  安全公告: https://example.com/security/2025-003

```

**"优雅砖化"和"静默接受攻击者代码"之间,前者永远是正确选择。** 关键是给出可执行的带外恢复路径,而不是丢一个 `signature verification failed` 让用户去 `--skip-verify`。

---

## 六、如果你现在不想上完整分层:一个廉价但真实有效的过渡方案

**Day-1 就生成 3 把密钥,但只启用 1 把:**

```go

var RootKeys = []VerifyKey{

    { ID: "k1", Pub: ..., Status: Active   },  // 在 KMS,日常签名
    { ID: "k2", Pub: ..., Status: Reserve  },  // 离线冷存,地点 A
    { ID: "k3", Pub: ..., Status: Reserve  },  // 离线冷存,地点 B
}

```

三把公钥**全部内置到客户端**,客户端接受其中任意一把的签名。k2/k3 的私钥从不上线、从不进 CI。

*   k1 泄露 → 直接切到 k2 签名,**所有老客户端立即接受,零砖化**
    
*   配合一条吊销规则:客户端一旦见到由 k2/k3 签署的、且被持久化的"吊销 k1"声明,就永久不再接受 k1
    

```json
{ "revoke": ["k1"], "not_before_index": 483, "signed_by": "k2" }

```

这本质上是"Root 内联在客户端里"的简化版:**成本几乎为零(多生成两个密钥、多存两份离线备份),但把"泄露=灾难"降级成"泄露=切换一次"。**

局限也要清楚:

*   储备密钥数量有限(3 次机会用完就必须靠二进制升级)
    
*   吊销声明仍可被扣住 → 所以\*\*`expires_at` 依然是必需的\*\*
    
*   没有透明日志,你可能长期不知道密钥被滥用
    

**建议路线:立刻做储备密钥(一天的工作量,别再拖),同时把** `**expires_at**` **+** `**index_version**` **单调性补上;下一个大版本引入 root.json 分层。** 对开源项目也可以直接跳到 Sigstore keyless——它连"长期私钥"这个东西都不存在,泄露场景自然消失(风险转移成 GitHub 账号/workflow 安全,那是另一套更成熟的防护)。

---

## 七、给你的架构建议:插件和核心分开授权

顺带解决你提到的"核心不升级但插件要升级"的耦合:

```plaintext
root.json
 ├─ role "core"    → Core Key    (签 CLI 自身的 manifest)
 ├─ role "plugins" → Plugin Key  (签插件索引)
 └─ role "vendor:acme" → 委派给第三方,只允许签 plugins/acme/* 路径

```

好处:

*   Plugin Key 泄露**不影响核心升级通道**,反之亦然,爆炸半径隔离
    
*   第三方插件作者可以用自己的密钥签,你通过 root 委派授权路径前缀,**不需要把私钥给他们、也不需要你代签**
    
*   撤销某个第三方作者 = 一次 root 更新,不影响其他任何人
    

\---## 一句话总结

> **你的直觉完全正确:硬编码单一公钥的方案在泄露时只有"砖化"和"裸奔"两个选项,没有第三条路。**解法不是改进轮换流程,而是改变信任锚的层级——**客户端钉住一个"几乎不用、多方阈值、离线保管的 Root",让实际签名密钥变成可在线替换的对象。** 这样"换私钥"就不再需要"换二进制",你的问题从根上消失。而 `expires` / `version` 单调性不是附加功能,它们是**让吊销能够抵达客户端的唯一途径**——没有它们,再完美的分层结构也会被"一直喂旧数据"这一招绕过。