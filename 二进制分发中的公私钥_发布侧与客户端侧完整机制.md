# 二进制分发中的公私钥:发布侧与客户端侧完整机制

# 二进制分发中的公私钥:发布侧与客户端侧完整机制

---

## 零、先纠正一个常见误解

很多人的心智模型是"私钥加密、公钥解密"。这个说法在现代签名体系里是**错的**:

*   **Ed25519 根本不是加密算法**,它只能签名/验签,没有"加密"这个操作。
    
*   RSA 虽然数学上双向可用,但 `RSASSA-PSS` 签名和 `RSA-OAEP` 加密是两套不同的填充方案,不能混谈。
    

正确的心智模型:

> **私钥 = 生成证据的能力**(只有持有者能产出); **公钥 = 校验证据的能力**(所有人都能做,且不泄露任何秘密)。签名是**不可伪造 + 可公开验证**的绑定关系:`(内容, 签名, 公钥) → 真/假`

关键推论:**公钥可以完全公开、随二进制分发给全世界,毫无风险。** 它甚至可以被攻击者读到——攻击者拿到公钥也造不出合法签名。这就是为什么"公钥硬编码进客户端"是安全的,而"私钥放在服务器上"是危险的。

---

## 一、全景图:谁持有什么

```plaintext
┌────────────── 发布侧 (你控制,高度受限) ──────────────┐
│                                                       │
│   私钥 (SIGNING KEY)                                  │
│   ├─ 存放: HSM / KMS / 离线机器 / CI OIDC 短期证书    │
│   ├─ 使用: 仅在 release 流水线的签名那一步            │
│   └─ 绝不出现在: 源码、CDN、容器镜像、日志            │
│                                                       │
│         │ sign(manifest)                              │
│         ▼                                             │
│   manifest.json + manifest.json.sig                   │
└───────────────────────┬───────────────────────────────┘
                        │
                        │  发布(推到任意不可信通道)
                        ▼
┌──────────── 分发通道 (视为完全不可信) ────────────────┐
│   CDN / S3 / GitHub Releases / 企业 Nexus / 镜像站    │
│   ★ 不持有任何密钥,不参与任何验证,纯哑管道 ★         │
└───────────────────────┬───────────────────────────────┘
                        │
                        │  下载
                        ▼
┌────────────── 客户端 (用户机器,不可信环境) ───────────┐
│                                                       │
│   公钥 (VERIFICATION KEY)                             │
│   ├─ 来源: 编译期硬编码进 CLI 二进制                  │
│   ├─ 数量: 1..N (支持轮换窗口)                        │
│   └─ 公开无害,可以被任何人读取/反编译看到             │
│                                                       │
│   verify(manifest, sig, pubkeys) → 通过后才信 manifest │
│   再用 manifest 里的 digest 校验实际二进制            │
└───────────────────────────────────────────────────────┘

```

**最重要的结构性特征:整个验证过程离线完成,服务端不参与。** 这点后面会和 TLS 对比。

---

## 二、发布侧(服务端)详解

### 2.1 密钥生成与保管:三档方案

**档位 A:本地长期密钥(小项目起步)**

```bash
# minisign:最简单,格式紧凑,Ed25519
minisign -G -p mycli.pub -s mycli.key
# 生成 ~200 字节公钥文件,私钥有 passphrase 保护

```
```bash
# 或纯 age/openssl 风格,Go 里自己生成
# pub 32 bytes, priv 64 bytes (Ed25519)

```

*   私钥存 CI Secret,**必须配合 GitHub Environment + required reviewers**,否则任何能触发 workflow 的人都能签任意东西。
    
*   缺点:密钥一旦泄露,你无法知道它被用过几次、签过什么。
    

**档位 B:KMS / HSM(推荐给商业产品)**

```bash
cosign sign-blob \
  --key awskms:///alias/mycli-release \
  --output-signature manifest.json.sig \
  manifest.json

```

*   **私钥永不离开 KMS**,CI 只有"调用签名 API"的权限,拿不到密钥材料本身。
    
*   有**审计日志**:每次签名调用都留痕,泄露后能查到被滥用的时间窗口。
    
*   可以设 IAM 条件:只允许特定 OIDC role、特定 tag 事件调用。
    

**档位 C:Keyless / Sigstore(推荐给开源)**

没有长期私钥。CI 用 OIDC token 向 Fulcio 换一张**有效期约 10 分钟**的证书,签完即弃:

```yaml
permissions:
  id-token: write   # 必须,否则拿不到 OIDC token
  contents: read

```
```bash
cosign sign-blob --yes \
  --output-signature manifest.sig \
  --output-certificate manifest.pem \
  manifest.json

```

*   **没有需要保管的私钥** → 消灭了"私钥泄露"这一整类风险。
    
*   签名事件写入 **Rekor 透明日志**,公开可审计;攻击者无法悄悄签一个"只发给某个受害者"的恶意版本而不留痕。
    
*   客户端钉住的不是公钥,而是**签名者身份**(见 3.2)。
    

### 2.2 签什么:不要逐文件签,要签 manifest

**反模式**:给 12 个平台的 12 个 tar.gz 各签一个 sig。问题:

*   12 次私钥调用,攻击面 ×12
    
*   签名内容里没有版本号、平台信息 → 无法防降级、无法防 mix-and-match
    
*   索引/插件列表本身仍然没被保护
    

**正确做法**:构造一份 manifest,把**所有需要被保护的语义**都写进去,然后只签这一份小文件。

```json
{
  "schema": 1,
  "product": "mycli",
  "release_version": "1.7.3",
  "index_version": 482,
  "created_at": "2025-06-02T08:11:00Z",
  "expires_at": "2025-06-16T08:11:00Z",
  "min_supported_client": "1.4.0",
  "artifacts": [
    {
      "name": "mycli",
      "version": "1.7.3",
      "os": "linux", "arch": "amd64",
      "path": "v1.7.3/mycli_1.7.3_linux_amd64.tar.gz",
      "size": 8123456,
      "sha256": "9f2c...e01a"
    },
    {
      "name": "mycli",
      "version": "1.7.3",
      "os": "darwin", "arch": "arm64",
      "path": "v1.7.3/mycli_1.7.3_darwin_arm64.tar.gz",
      "size": 8011234,
      "sha256": "13ab...77cd"
    }
  ],
  "plugins": [
    { "name": "fmt", "version": "0.9.1", "os": "linux", "arch": "amd64",
      "path": "plugins/fmt/0.9.1/fmt_linux_amd64.tar.gz",
      "sha256": "aa77...92f1" }
  ]
}

```

一份签名同时提供了:

| manifest 字段 | 挡住的攻击 |
| --- | --- |
| `sha256` | 内容篡改 |
| `os` / `arch` / `name` | **mix-and-match**(把 linux 包冒充 darwin 包、把 A 插件冒充 B 插件) |
| `index_version` 单调递增 | **回滚攻击**(重放旧的合法 manifest) |
| `expires_at` | **冻结攻击**(一直喂旧 manifest 让你永远升不到修复版) |
| `min_supported_client` | 强制淘汰有漏洞的老客户端 |
| `size` | 解压炸弹 / 资源耗尽的粗过滤 |

### 2.3 签名的技术细节:为什么只签几百字节

`crypto/ed25519` 的 `Sign()` 内部会对**整个 message** 做 SHA-512。如果直接签一个 8MB 的 tar.gz,意味着私钥操作要吃进 8MB 数据——KMS/HSM 场景下这很难受(网络传输、API 大小限制)。

标准做法是**两级摘要**:

```plaintext
文件(8MB) ──SHA-256──> digest(32B) ──写入──> manifest(~2KB) ──sign──> sig(64B)

```

私钥只处理 2KB。这也是 apt 的 `Release`/`Release.gpg`、Docker 的 image manifest、TUF 的 `targets.json` 全都采用的结构。

### 2.4 完整流水线示例

```yaml
name: release
on:
  push:
    tags: ['v*']

permissions:
  contents: write
  id-token: write        # keyless 签名必需
  attestations: write

jobs:
  release:
    runs-on: ubuntu-latest
    environment: production          # ← 加人工审批门禁
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }

      - name: Build (reproducible)
        run: |
          # 可复现构建:去掉路径、去掉时间戳
          export CGO_ENABLED=0
          export SOURCE_DATE_EPOCH=$(git log -1 --format=%ct)
          for target in linux/amd64 linux/arm64 darwin/amd64 darwin/arm64 windows/amd64; do
            GOOS=${target%/*} GOARCH=${target#*/} \
            go build -trimpath \
              -ldflags="-s -w -buildid= -X main.version=${GITHUB_REF_NAME}" \
              -o dist/mycli_${GOOS}_${GOARCH}${EXT} ./cmd/mycli
          done

      - name: Package & build manifest
        run: ./scripts/make-manifest.sh > dist/manifest.json

      - uses: sigstore/cosign-installer@v3

      - name: Sign manifest (keyless)
        run: |
          cosign sign-blob --yes \
            --output-signature dist/manifest.json.sig \
            --output-certificate dist/manifest.json.pem \
            dist/manifest.json

      # 额外一层:SLSA provenance,证明"这个产物由这个 commit 在这个 workflow 构建"
      - uses: actions/attest-build-provenance@v1
        with:
          subject-path: 'dist/mycli_*'

      - name: Publish
        run: aws s3 sync dist/ s3://dl.example.com/v${VERSION}/
        # 注意发布顺序:先传制品,最后传 manifest+sig
        # 否则客户端可能拿到指向尚不存在文件的 manifest

```

**发布顺序很重要**:`artifacts → manifest → sig`(或 manifest 和 sig 原子替换)。反过来会造成短暂的"manifest 指向 404"窗口。

---

## 三、客户端侧详解

### 3.1 公钥如何进入二进制

**方式一:源码常量(最简单、最推荐)**

```go
package trust

// 编译期固定。这不是秘密,可以公开在 GitHub 上。
// 顺序有意义:第一个是当前主密钥。

var RootKeys = []VerifyKey{

    {
        ID:     "2025-key-a",
        Algo:   "ed25519",
        PubKey: mustDecodeHex("d75a980182b10ab7d54bfed3c964073a0ee172f3daa62325af021a68f707511a"),
        NotAfter: mustTime("2027-01-01T00:00:00Z"),
    },
    {   // 上一代密钥,轮换过渡窗口内仍接受
        ID:     "2023-key-a",
        Algo:   "ed25519",
        PubKey: mustDecodeHex("3d4017c3e843895a92b70aa74d1b7ebc9c982ccf2ec4968cc0cd55f12af4660c"),
        NotAfter: mustTime("2025-12-31T00:00:00Z"),
    },
}

```

**方式二:**`**go:embed**` **一个公钥文件**

```go
//go:embed keys/root.pub keys/root-prev.pub
var keyFS embed.FS

```

**方式三:**`**-ldflags -X**` **注入**——⚠️ **不要用于公钥**。这让"哪个公钥被编进去了"取决于构建时的环境变量,容易被 CI 配置错误或供应链投毒篡改,且无法通过 code review 发现。公钥应该在 git 里、能被 review、能被 diff。

**绝对禁止的做法**:

```go
// ❌ 灾难:运行时从分发渠道取公钥,等于完全没签
pub, _ := http.Get(baseURL + "/release.pub")

// ❌ 灾难:从可写配置文件读公钥,恶意 PR 塞一行就能接管
pub := config.GetString("verify.pubkey")

// ❌ 灾难:签名里自带证书就无条件信任(自签名任何人都能造)
cert := parseCert(sigFile.EmbeddedCert)   // 没有钉住 identity

```

### 3.2 Keyless 模式下客户端钉什么

Sigstore 场景没有长期公钥,客户端要钉的是**身份约束**,同样硬编码:

```go
var TrustPolicy = SigstorePolicy{
    // 只接受这个 repo 的这个 workflow 在 tag 上的构建
    CertIdentityRegexp: `^https://github\.com/myorg/mycli/\.github/workflows/release\.yml@refs/tags/v`,
    CertOIDCIssuer:     "https://token.actions.githubusercontent.com",
    RequireRekor:       true,   // 必须在透明日志中存在
}

```

**这是最容易踩的坑**:只调 `cosign verify` 而不指定 `--certificate-identity` / `--certificate-oidc-issuer`,等于"接受任何人的签名"——攻击者用自己的 GitHub 账号也能签出一个 Sigstore 上完全合法的签名。**没有 identity 约束的 keyless 验证 = 没有验证。**

### 3.3 客户端验证流程(顺序至关重要)

```plaintext
1. 下载 manifest.json + manifest.json.sig            (内容暂时视为敌对)
2. 验签: verify(manifest_bytes, sig, RootKeys)        ← 第一道闸门
   ✗ 失败 → 立即中止,不解析 JSON
3. 解析 manifest(此时才允许信任其内容)
4. 新鲜性检查:
   - now < expires_at                                 (防冻结)
   - index_version >= 本地记录的最大值                (防回滚)
   - 本地版本 >= min_supported_client                 (强制淘汰)
5. 查表:按 name+version+os+arch 精确匹配 → 得到 expected sha256
   ✗ 找不到 → 中止(不要 fallback 到"猜路径")
6. 下载制品,流式计算 SHA-256,同时限制最大字节数
7. 常量时间比对 digest
   ✗ 不匹配 → 删除临时文件,中止
8. 此时才解压 / 才写入最终路径 / 才 chmod +x
9. 持久化 index_version 到本地状态

```

**两个高频错误**:

*   **先解压后验签**:攻击者可以用 zip slip(`../../../.ssh/authorized_keys`)或解压炸弹在你验签之前就已经完成攻击。**验签必须在任何解析/解压/执行之前**。
    
*   **原地覆盖再校验**:自升级时如果先把新二进制写到最终路径再校验,失败时已经破坏了可用状态。正确做法:下载到临时文件 → 验证 → `rename()` 原子替换。
    

### 3.4 参考实现

```go
package update

import (
    "crypto/ed25519"
    "crypto/sha256"
    "crypto/subtle"
    "encoding/hex"
    "errors"
    "fmt"
    "io"
    "os"
    "time"
)

var (
    ErrBadSignature   = errors.New("manifest signature verification failed")
    ErrExpired        = errors.New("manifest expired; possible freeze attack")
    ErrRollback       = errors.New("manifest version regressed; possible rollback attack")
    ErrDigestMismatch = errors.New("artifact digest mismatch")
    ErrNoMatch        = errors.New("no artifact for this platform/version")
)

const maxManifestSize = 8 << 20   // 8MB
const maxArtifactSize = 512 << 20 // 512MB

func FetchVerified(src Source, name, version, goos, goarch string) (path string, err error) {
    // ── 1. 取 manifest 与签名(此刻都是不可信字节) ─────────────
    rawManifest, err := src.Get("manifest.json", maxManifestSize)
    if err != nil { return "", err }
    rawSig, err := src.Get("manifest.json.sig", 4096)
    if err != nil { return "", err }

    // ── 2. 第一道闸门:任何解析之前先验签 ────────────────────
    if err := verifyAny(rawManifest, rawSig, trust.RootKeys); err != nil {
        return "", fmt.Errorf("%w (source=%s)", ErrBadSignature, src.BaseURL)
    }

    // ── 3. 现在才允许信任内容 ────────────────────────────────
    var m Manifest
    if err := strictJSONUnmarshal(rawManifest, &m); err != nil {
        return "", err   // 用 DisallowUnknownFields,防降级解析歧义
    }

    // ── 4. 新鲜性 / 单调性 ───────────────────────────────────
    now := time.Now().UTC()
    if now.After(m.ExpiresAt) {
        return "", fmt.Errorf("%w (expired_at=%s)", ErrExpired, m.ExpiresAt)
    }
    if seen, _ := loadLastIndexVersion(); m.IndexVersion < seen {
        return "", fmt.Errorf("%w (got=%d, seen=%d)", ErrRollback, m.IndexVersion, seen)
    }
    if semverLess(currentVersion, m.MinSupportedClient) {
        return "", fmt.Errorf("client too old, minimum is %s", m.MinSupportedClient)
    }

    // ── 5. 精确匹配,不做路径推测 ────────────────────────────
    a := m.Find(name, version, goos, goarch)
    if a == nil { return "", ErrNoMatch }

    // ── 6/7. 流式下载 + 边算 hash + 限长 ────────────────────
    tmp, err := os.CreateTemp("", "mycli-dl-*")
    if err != nil { return "", err }
    defer func() {
        tmp.Close()
        if err != nil { os.Remove(tmp.Name()) }   // 失败不留残渣
    }()

    body, err := src.Open(a.Path)
    if err != nil { return "", err }
    defer body.Close()

    h := sha256.New()
    n, err := io.Copy(io.MultiWriter(tmp, h), io.LimitReader(body, maxArtifactSize+1))
    if err != nil { return "", err }
    if n > maxArtifactSize || n != a.Size {
        return "", fmt.Errorf("size mismatch: got %d want %d", n, a.Size)
    }

    want, _ := hex.DecodeString(a.SHA256)
    if subtle.ConstantTimeCompare(h.Sum(nil), want) != 1 {
        return "", fmt.Errorf("%w (%s)", ErrDigestMismatch, a.Path)
    }

    // ── 8/9. 通过后才落盘、才记录状态 ───────────────────────
    saveLastIndexVersion(m.IndexVersion)
    return tmp.Name(), nil
}

// 多公钥:支持轮换窗口

func verifyAny(msg, sig []byte, keys []trust.VerifyKey) error {

    now := time.Now().UTC()
    for _, k := range keys {
        if now.After(k.NotAfter) { continue }
        if ed25519.Verify(ed25519.PublicKey(k.PubKey), msg, sig) {
            return nil
        }
    }
    return ErrBadSignature
}

```

自升级的原子替换部分:

```go
func SelfReplace(newBin string) error {
    exe, err := os.Executable()
    if err != nil { return err }
    exe, _ = filepath.EvalSymlinks(exe)

    if err := os.Chmod(newBin, 0o755); err != nil { return err }

    // Windows 不允许覆盖正在运行的 exe:先改名自己,再放新的
    if runtime.GOOS == "windows" {
        old := exe + ".old"
        os.Remove(old)
        if err := os.Rename(exe, old); err != nil { return err }
        if err := os.Rename(newBin, exe); err != nil {
            os.Rename(old, exe)   // 回滚
            return err
        }
        return nil
    }
    // POSIX: rename 是原子的,同分区前提下
    return os.Rename(newBin, exe)
}

```

### 3.5 Fail-closed 的具体含义

```go
// ✅ 正确
if err := verify(...); err != nil {
    return err   // 中止。没有 else 分支,没有降级路径
}

// ❌ 常见的自我阉割
if err := verify(...); err != nil {
    log.Warn("signature check failed, continuing anyway")  // 等于没做
}

// ❌ 网络错误被当成"可跳过"
sig, err := src.Get("manifest.json.sig")
if err != nil {
    // 攻击者只要让 .sig 返回 404,就绕过了整套机制
    return install(artifact)   // 灾难
}

```

**签名文件缺失必须等同于签名错误。** 这是"降级攻击"(downgrade attack)的经典入口:攻击者不需要伪造签名,只需要让签名"消失"。

---

## 四、为什么 HTTPS/TLS 不能替代这一层

这是理解整套设计的关键对比:

|  | TLS(HTTPS) | 制品签名 |
| --- | --- | --- |
| 保护对象 | **传输通道** | **内容本体** |
| 私钥位置 | **在线服务器上**(必须,用于握手) | **离线 / HSM / 短期证书** |
| 服务器被攻破 | 拿到 TLS 私钥,可完美冒充 | 拿不到签名私钥,无法签新内容 |
| 有效期 | 仅连接期间 | 永久(签名可离线保存和复验) |
| 经过代理/镜像/缓存后 | 保护**失效**(TLS 在此终止) | 保护**保持**(签名跟着字节走) |
| 能否防"发布方自己发错了" | 不能 | 部分能(透明日志可审计) |

具体场景:

*   用户公司用 Nexus 代理你的 registry。TLS 在 Nexus 处终止,Nexus 重新用自己的证书对内提供服务。**你的 TLS 保护到 Nexus 就没了**,Nexus 里被污染的缓存,客户端毫无察觉。有签名的话,Nexus 无论怎么改都会验签失败。
    
*   你的 S3 bucket 因为一个 IaC 错误变成了 public-write。TLS 完好无损地把恶意文件加密传给了每个用户。签名会挡住。
    
*   攻击者拿到你的 CDN 控制台。TLS 证书是他的,一切正常。签名私钥不在 CDN 上,挡住。
    

**结论:TLS 保护"路径",签名保护"字节"。前者在多跳架构中必然失效,后者端到端有效。**

---

## 五、密钥生命周期:轮换与吊销

### 5.1 轮换(计划内)

长期密钥轮换的难点是**鸡生蛋**:客户端只认老公钥,你怎么让它接受新公钥?

标准解法是**重叠窗口 + 双签**:

```plaintext
T0   生成 Key-B。发布 v2.0,二进制内置 [Key-A(主), Key-B]
     manifest 仍只用 Key-A 签  → 老客户端(只认 A)正常工作

T1   manifest 用 A 和 B 双签   → 两代客户端都能验证
     (客户端逻辑:任一公钥验过即通过)

T2   绝大多数用户已升到 v2.0+。切换为仅用 Key-B 签
     发布 v2.5,内置 [Key-B(主), Key-A(标记 NotAfter=T3)]

T3   Key-A 的 NotAfter 到期,客户端自动停止接受。销毁 Key-A

```

窗口长度取决于你的用户升级曲线,CLI 工具通常 6–12 个月比较现实。

**更优雅的做法是分层(TUF 的核心思想)**:

```plaintext
Root Key (离线,5 年有效,极少使用)
   └─ 签 → "当前有效的 Signing Key 列表" (root.json)
              └─ Signing Key (在线/KMS,90 天轮换)
                    └─ 签 → manifest.json

```

客户端只硬编码 **Root 公钥**。Signing Key 可以频繁轮换、被吊销、增删,客户端通过验证 root.json 自动学习到新的 signing key,**无需升级二进制**。代价是多一层复杂度和一份需要保护的离线密钥。

### 5.2 吊销(计划外,私钥泄露了)

这是最难的部分,因为**离线验证天生无法实时查询吊销状态**。可用手段:

1.  `**min_supported_client**`:在新 manifest 里把门槛抬高,老客户端一旦联网就拒绝继续工作,强制升级。
    
    *   局限:攻击者若能持续喂旧 manifest,受害者永远看不到新的门槛值。这就是 `expires_at` 的价值——旧 manifest 会自然失效,客户端进入"无法获得有效 manifest"的 fail-closed 状态。
        
2.  `**expires_at**` **短周期**(7–14 天):把攻击窗口从"永久"压缩到"两周"。这是防冻结的核心,也是吊销能生效的前提。
    
3.  **Root 层吊销**:如果用了分层结构,用 Root Key 签一份新 root.json 移除被泄露的 signing key。客户端拿到新 root.json 后立即生效。
    
4.  **透明日志监控**:Sigstore/Rekor 场景下,你可以订阅监控,一旦出现"不是我发起的签名"就能在几分钟内发现。这是长期密钥方案完全不具备的能力。
    
5.  **应急:直接改 URL / 停服**。粗暴但有效——让老通道返回 410,配合公告。
    

> 现实建议:**如果你的产品有商业客户,直接上分层结构或 Sigstore。** 只用单个长期密钥的方案在密钥泄露时基本没有优雅出路,只能靠"发公告求用户手动重装"。

---

## 六、两种模式并列对比

| 维度 | 长期密钥(minisign / KMS) | Keyless(Sigstore) |
| --- | --- | --- |
| 客户端硬编码什么 | 公钥字节 | identity regexp + OIDC issuer + Fulcio/Rekor 根 |
| 私钥管理负担 | 高(要保管、轮换、防泄露) | **无长期私钥** |
| 离线/气隙验证 | ✅ 完全离线 | ⚠️ 需要 bundle 内嵌 Rekor 证据才能离线 |
| 可审计性 | ❌ 无法知道密钥被用过几次 | ✅ 全球公开透明日志 |
| 依赖外部服务 | 无 | 签名时依赖 Fulcio/Rekor 可用性 |
| 轮换成本 | 高(需要重叠窗口) | 无(证书本来就 10 分钟过期) |
| 适合 | 商业闭源、气隙环境、二进制体积敏感 | 开源项目、GitHub Actions 流水线 |

**很多成熟项目两者都做**:Sigstore 作为主验证路径(可审计),同时提供一份长期密钥签名作为离线/降级路径。

---

## 七、实施自查清单

**发布侧**

*   [ ] 私钥是否**从不出现**在源码库、CDN、容器镜像、CI 日志里?
    
*   [ ] 签名步骤是否有 environment 门禁 / 人工审批 / 仅 tag 触发?
    
*   [ ] 签的是 **manifest**,且 manifest 包含 
    
*   [ ] 
    
*   [ ] 发布顺序是 artifacts → manifest → sig(避免指向 404 的窗口)?
    
*   [ ] 是否有独立的 canary 作业,发布后从**外部网络**重新完整验一遍?
    

**客户端侧**

*   [ ] 公钥/identity policy 是否**编译期固定**,且在 git 中可 review?
    
*   [ ] 是否**绝无任何**从配置文件/环境变量/网络读取信任锚的代码路径?
    
*   [ ] keyless 是否**约束了 identity + issuer**(不只是 
    
*   [ ] 验签是否发生在**解析 JSON、解压、chmod、执行之前**?
    
*   [ ] 
    
*   [ ] 是否检查了 
    
*   [ ] 是否精确匹配 
    
*   [ ] 下载是否**限长**、是否**流式计算 hash**(不整包读内存)?
    
*   [ ] digest 比对是否用了常量时间比较?
    
*   [ ] 自升级是否**下载到临时文件 → 验证 → 原子 rename**?
    
*   [ ] Windows 的"无法覆盖运行中 exe"是否处理了,且失败能回滚?
    
*   [ ] 跳过验签是否**只能靠显式命令行 flag**、有醒目告警、并记录进 lockfile?
    

---

## 八、一句话总结

> **私钥的唯一职责是"在一个尽可能受限、尽可能短暂的时刻,为一份小小的 manifest 产出 64 字节证据";公钥的唯一职责是"以编译期固化、不可被配置篡改的形式,在客户端拒绝一切没有该证据的字节"。**中间的 CDN、镜像、代理、企业仓库,全部可以是敌人——这正是设计目标,而不是妥协。